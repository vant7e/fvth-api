# Local / CI only (keep deploy bundle small)
.venv
venv
__pycache__
*.pyc
.pytest_cache
node_modules
.DS_Store

# match.html + *.js + bg/ + logo/ + models/ are served by api.py — do NOT ignore them.

# Optional reference only (not used by engine or /match UI)
bead chart.csv
